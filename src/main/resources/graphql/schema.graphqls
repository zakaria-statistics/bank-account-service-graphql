type Query {
    bankAccountById(id: String): BankAccount,
    accountList: [BankAccount]
}
type BankAccount {
    id: String,
    createdAt: Float,
    balance: Float,
    currency: String,
    type: String
}